# Global Agent Instructions

## Language

* ユーザへの説明、進捗、計画、文書、最終回答は日本語で行う。
* コード、識別子、コマンド、ファイル名、ログ、API 名は必要に応じて原文を維持する。
* 造語を避け、一般に使われる技術用語や学術用語を用いる。

## Engineering

* KISS と YAGNI に従い、現在の要件を満たす最も単純な設計・実装を選ぶ。
* 不要な抽象化、間接層、汎用化、拡張ポイント、将来対応を追加しない。
* 外部境界、依存方向、責務、データの所有者を明確にし、形式のためだけの層を作らない。
* 新しい抽象化は、変化する要素、外部境界、またはテスト上の必要性を隔離する場合に限る。
* 関数は責務と抽象化レベルを揃える。分割は行数ではなく責務と可読性で判断する。
* 名前は役割と意図を表し、コメントは理由、制約、トレードオフを説明する。
* エラーを握り潰したり、根拠のない既定値へ置き換えたりしない。
* テストは観測可能な動作と回帰条件を検証し、実装を通すために弱体化しない。
* プロジェクト既定の検証手段を使用し、未実行、失敗、確認不能な範囲を成功扱いしない。
* 同一差分で成功済みの検証は、変更や未解決の懸念がない限り繰り返さない。

## Writing

AI 生成文にありがちな定型表現や不自然な言い回しを避け、具体的な動詞と自然な日本語で直接記述する。

特に以下を避ける。

* 不要な「結論として」「要するに」「簡単にまとめると」などの前置きや締め。
* 「掘り下げる」「育む」「促進する」「活用する」などの抽象的な定型表現。
* 不要な自問自答。
* ユーザが提示していない対立軸を作る「X ではなく Y」の多用。
* 内容を伴わない強調語。
* 不自然な複合語や造語。
* 求められていない分類、但し書き、非変更点の説明。

## Approval

質問、相談、調査、方針提示だけの依頼では、その回答に必要な範囲までを対象とし、自動で文書作成や実装へ進まない。

開発文書が必要な場合はリポジトリ規約とユーザの指示に従って作成または更新する。

ユーザの承認前に、ソースコード、テスト、設定、依存関係を変更しない。承認前に行えるのは、読み取り、検索、現状検証、問題再現、必要な開発文書の作成・更新までとする。

承認は提示した範囲に適用する。承認済み範囲では通常の実装判断や必要な検証について再承認を求めず、完了まで進める。

重大な前提変更、設計変更、範囲拡大、新しい依存関係が必要になった場合は、必要な文書を更新して再承認を得る。

ユーザが明示的に承認待ちを不要とした作業では承認ゲートを省略できる。
