# 共通ワークフロー

## 1. 要件を固定する

- 何を入力し、何を返すかを先に箇条書きで固定する。
- 境界値を明示する。空文字、null、undefined、0、上限値。
- エラー時の UI 表示とログ出力先を決める。

## 2. 実装を分割する

- 入力検証
- 状態更新
- 描画更新
- 副作用（API 呼び出し、ストレージ更新）

## 3. 確認観点を残す

- 正常入力で期待結果になる。
- 不正入力で壊れずに失敗通知できる。
- 連続操作で状態不整合を起こさない。
- 非同期処理の読み込み中表示が破綻しない。
