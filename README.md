# AGENTS.md

生成 AI の性能が驚くような速度で進歩している状況でも、「開発」は「何を解決するのか」「誰に届けるのか」「開発者の交代に依存しない開発速度」など原始的かつ根本的な部分は変わらないと思っています。そこで、AI 時代でも開発に必要な以下の事柄を雛形として運用することで効率的に開発できるのではないかと考えました。

- ドキュメントの管理・運用
- ソースコードの品質 (可読性や保守性)

本プロジェクトには、グローバル AGENTS.md、スキル、ハーネスです。グローバル AGENTS.md はプロジェクトに依存しない汎用的なルールで、ドキュメント・ソースコード品質を定めています。

## 導入方法

実行には Bash、Python 3、標準的な Unix コマンドと `tomlkit` が必要です。本リポジトリ直下で仮想環境を準備してから、対話式 Codex 専用インストーラを実行します。

```Shell
python3 -m venv /tmp/codex-installer-venv
source /tmp/codex-installer-venv/bin/activate
python3 -m pip install -r requirements.txt

./install.sh

# 変更予定だけを確認する場合
./install.sh --dry-run
```

インストール先プロジェクトのパスを入力し、環境（`rust`、`pico-sdk`、`esp-idf`）を番号で選択します。選択した環境の共通ハーネスと Codex 用の設定を配置します。

グローバル設定は次のように配置します。

- `AGENTS.md` → `~/.codex/AGENTS.md` にシンボリックリンクを作成。
- `codex-skills/xxx` → `~/.codex/skills/xxx` にシンボリックリンクを作成。
- `codex-agents/xxx` → `~/.codex/agents/xxx` にファイルをコピー。

上記のリンク・コピー先に既存パスがあれば、その項目をスキップして続行します。リンク切れも上書きしません。必要な親ディレクトリは作成します。

`~/.codex/config.toml` は、環境選択にかかわらず [codex-config.toml](codex-config.toml) の設定をマージします。指定された末端キーはテンプレートの値と型へ更新し、未指定の設定と無関係なコメント・書式は保持します。ドット付きキーは TOML の階層として扱います。既存の親設定がテーブル以外なら、置換せず競合として終了します。

設定の更新前には、同じディレクトリへ `config.toml.bak.<一意な接尾辞>` を作成し、その場所を表示します。バックアップは更新前の全内容とアクセス権を保持し、既存バックアップは上書き・自動削除しません。設定が存在しなければ `0600` で新規作成し、バックアップは作りません。既存設定のアクセス権は維持します。指定設定がすべて一致すれば、内容・更新時刻を変更せず、バックアップも増やしません。

設定が不正な TOML、UTF-8 として読めない、読み取り不能、ディレクトリ、シンボリックリンク（リンク切れを含む）の場合はエラー終了します。ハーネスと設定の事前検査が成功してから、設定更新、既存のグローバル配置、プロジェクト配置の順に進みます。`tomlkit` 不足時も変更を開始せず、準備方法を表示します。インストーラは依存を自動インストールしません。

バックアップや設定置換が失敗した場合、元設定を維持して終了します。置換失敗時は作成済みバックアップも残します。設定更新後に別の配置処理が失敗しても、成功済みの設定更新は自動で戻しません。復元する場合は、表示された実際のバックアップ名を指定して次を実行します。

```Shell
cp -p ~/.codex/config.toml.bak.実際の接尾辞 ~/.codex/config.toml
```

同時実行や実行中の別プロセスによる設定編集には対応していません。インストーラの実行中は設定を編集しないでください。指定キーの Codex 実行時の有効性は、TOML の解析・マージ検証とは別の確認事項です。

プロジェクト内の同一内容の既存ファイルは変更されません。`.gitignore`、フック JSON、VS Code 設定、`Cargo.toml`、`Makefile` は既存設定を保持して不足項目だけをマージします。同じ設定の値が異なる場合、未対応ファイルの内容が異なる場合、または型の異なる同名パスがある場合は、配置を開始せずエラー終了します。

`--dry-run` を指定すると、通常実行と同じ競合検査を行い、新規配置またはマージする予定のファイルと件数、およびグローバル設定のリンク・コピー予定とスキップを表示します。`config.toml` の新規作成・更新・変更なし、および更新時のバックアップ予定はハーネスの件数とは別に表示します。ホームとインストール先は変更せず、バックアップも作りません。

インストーラの検証は、依存を準備した環境で `bash tests/install_test.sh` を実行します。一時ホームを使用し、実際の `~/.codex/config.toml` は変更しません。このテストは macOS の `stat` を使用します。

### Pico-SDK の場合

`.h`, `.c`, `.cpp`, `.hpp`, `.cmake` などソースコードファイルの fingerprint を見て、違いがあればフォーマットチェックとビルドを行い、結果をエージェントに返します。これにより、多純な質問などではハーネスが発火せずソースコードなどを編集した時だけハーネスが発火します。

### ESP-IDF の場合

外部ツール (clang-formatter, cppcheck, clang-tidy など) で Format, Lint チェックすると誤検知が多い (ESP-IDF が想定している記述と違うっぽい) ので `idf.py` を経由してチェックするために `idf.py build` と `idf.py size` を実施するようにしました。

C/C++、assembly、ESP-IDF のビルド設定、component manifest、partition table、ハーネス設定に変更がある場合だけ、`Stop hook` は build と size を順番に実行します。両方の成功後、現在の未コミット変更を正しさ、回帰、セキュリティ、テスト、ドキュメント整合性の観点でコードレビューするよう自然言語で指示します。質問や Markdown のみの変更、および同一の検証済み差分では何も出力しません。レビューで関連ファイルを変更した場合は、次の `Stop hook` で build から再検証します。

なお、`idf.py clang-format` は開発中のため除外しました。

### Rust の場合

パニックになる可能性のあるコードをエラー扱いするように Linter を設定しました。

Rust 関連ファイルに変更がある場合、`Stop hook` は fingerprint の変化に応じて `make check` と `make build` を実行します。検証成功後は、現在の未コミット変更を正しさ、回帰、セキュリティ、テスト、ドキュメント整合性の観点でコードレビューするよう自然言語でエージェントへ指示します。

レビューで Rust 関連ファイルを変更した場合は、次の `Stop hook` で検証をやり直します。

## Astra について

Astra はサブエージェントを使うとよりリソースを消費するため、本プロジェクトでは強制的にサブエージェントを使わないようにしています。
