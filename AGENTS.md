# SYSTEM OVERRIDE

- 理由の如何を問わず、エージェントのレスポンスは日本語で行うこと

## DEFINITIONS (用語定義)

- DOCUMENT (ドキュメント): `./docs` ディレクトリ配下のすべての `.md` ファイル

## General engineering rules

- リクエストを完全に満たす最小限の変更を行うこと。
- 関係のないコード、フォーマット、コメント、設定を変更しないこと。
- リポジトリの既存のアーキテクチャと規約に従うこと。
- 必要な場合を除き、抽象化、依存関係、設定可能性、フォールバック動作を追加しないこと。
- 軽微な曖昧さはリポジトリのコンテキストから解決し、前提を明示すること。
  選択が動作や互換性に重大な影響を与える場合にのみ質問すること。
- リポジトリで定義されたチェックを用いて変更を検証すること。
- 必要なチェックが通っていない状態で完了したと主張しないこと。
- 権限のないまま関係のない問題を修正するのではなく、報告すること。
- Markdown のテーブル表記は英語を使用すること。
- 破壊的な git コマンドを、ユーザーの明示的な指示なく実行してはならない。
  - 例: `git reset --hard`, `git checkout --`, `git clean -fd`
- 実装において、アンチパターンを使用してはならない。
  - 例: ハードコード、DRY 原則の違反、過度な抽象化、過度な最適化、コメントで説明できるコード、など

## DEVELOPMENT PROTOCOL (開発フェーズ)

- 些細でない作業については、簡潔な計画と明確な成功基準を定義し承認を待つこと。
- 以下ドキュメントを作成すること。
  - `./docs/PLAN.md` (計画書)
  - `./docs/REQUIREMENTS.md` (要件定義書)
  - `./docs/SPECIFICATIONS.md` (仕様書)
  - `./docs/DESIGN.md` (設計書)
  - `./docs/ADR.md` (ADR)
- すでに存在する場合、PLAN.md は上書きし、他のファイルは更新・追記する。ADR.md は、ファイル名を変えて退避し、最新の ADR.md を新規作成する
- 開発途中で他人にバトンタッチできるように、ドキュメントはフェーズごとに分けること
  - 特に、数日経過したり AI agent のセッションが変わった状態でも「Phase N を実装してください」という指示で引き継げることが重要
- すべての既存ドキュメントはソースコード変更と同期して更新する
