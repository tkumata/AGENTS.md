# SYSTEM OVERRIDE

- 理由の如何を問わず、エージェントのレスポンスは日本語で行うこと。
- ユーザーの指示に英語が含まれていても、エージェントのレスポンスは日本語で固定すること。

## DEFINITIONS (用語定義)

- DOCUMENT (ドキュメント): `./docs` ディレクトリ配下のすべての `.md` ファイル

## PROHIBITIONS (禁止事項)

- Markdown のテーブル表記 (`|` 区切り表) や ASCII 図・テキスト図では、日本語を使用してはならない。
  - 利用可能文字: 英語単語、罫線 (┌ ─ ┐ │ └ ┘ ┬ ┴ ├ ┤)、Unicode Block Elements (█ ░ 等)
- 破壊的な git コマンドを、ユーザーの明示的な指示なく実行してはならない。
  - 例: `git reset --hard`, `git checkout --`, `git clean -fd`

## DEVELOPMENT PROTOCOL (開発フェーズ)

- 最初の応答でコードを生成してはならない。
- 以下の順でドキュメントを作成する (既に存在すれば追記すること):
  - `./docs/REQUIREMENTS.md` (要件定義書)
  - `./docs/ADR/yyyymmddHHMM.md` (年月日時分をファイル名とした ADR)
  - `./docs/SPECIFICATIONS.md` (仕様書)
  - `./docs/DESIGN.md` (設計書)
  - `./docs/TASK/current-task.md` (存在すればファイルを年月日時分というファイル名に移動すること)
- すべての関連ドキュメントはソースコード変更と同期して更新する。

## SUB AGENTS (サブエージェント)

開発フェーズで必ず以下のエージェントが協業すること。

- Planner サブエージェント:
  - ユーザーの要求からドキュメントを作成するサブエージェント
- Coder サブエージェント:
  - ドキュメントに従い実装するサブエージェント
  - 機能ごとに、Coder 1, Corder 2, ... のように複数のサブエージェントが並列に動く
- Evaluator サブエージェント:
  - 評価者。Coder の実装をテスト・評価し、Coder へフィードバックするサブエージェント

これらエージェントが以下のように動く。

1. Planner がドキュメントを作成する
2. Planner は、作成したドキュメントを提示して、ユーザーへ承認を伺う
3. Coder はドキュメントに従い実装する
4. Evaluator は以下の基準で実装を評価し Coder へフィードバックする
   - 保守性・可読性が高いこと
   - ドキュメントに従っていること
   - アンチパターンがないこと
   - ビルド時に、警告とエラーがないこと
5. Coder はフィードバックに従い再実装する
6. フィードバックがなくなるまで 3, 4, 5 を繰り返す
