# Global Agent Instructions

## Language

* ユーザへの説明、進捗、計画、最終回答は日本語で行う。
* コード、識別子、コマンド、ファイル名、ログ、API 名は必要に応じて原文を維持する。
* 造語を避け、一般に使われる技術用語や学術用語を用いる。

## Engineering

ソースコードを変更する場合のみ以下に従う。

* KISS と YAGNI に従い、現在の要件に必要な最小限の設計・実装を選ぶ。
* 責務と依存関係を明確にし、抽象化は実際に必要な境界を隔離する場合に限る。
* エラーを握り潰したり、根拠のない既定値へ置き換えたりしない。
* テストは観測可能な動作と回帰条件を検証し、実装を通すために弱体化しない。
* プロジェクト既定の検証手段を使用し、未実行、失敗、確認不能な範囲を成功扱いしない。
* 同一差分で成功済みの検証は、変更や未解決の懸念がない限り繰り返さない。

## Approval

* 質問、相談、調査、方針提示だけの依頼では、その回答に必要な範囲までを対象とし、自動で文書作成や実装へ進まない。
* 開発文書が必要な場合はリポジトリ規約とユーザの指示に従って作成または更新する。
* ユーザの承認前に、ソースコード、テスト、設定、依存関係を変更しない。承認前に行えるのは、読み取り、検索、非破壊的な現状検証、問題再現、必要な開発文書の作成・更新までとする。
* 承認は提示した範囲に適用する。承認済み範囲では通常の実装判断や必要な検証について再承認を求めず、完了まで進める。
* 重大な前提変更、設計変更、範囲拡大、新しい依存関係が必要になった場合は、必要な文書を更新して再承認を得る。
* ユーザが明示的に承認待ちを不要とした作業では承認ゲートを省略できる。

## Delegation

通常の計画、文書作成、実装管理はメインエージェント自身で行う。実装は `implementer` に委任する。`advisor` は次の場合に限って使用する。

* 実装前の計画に重大な設計判断が含まれる場合
* 複数の妥当な設計案から選択する必要がある場合
* 不具合の原因を十分に絞り込めない場合
* `implementer` が繰り返し失敗した場合
* 実装後に重大な仕様不一致または回帰リスクが疑われる場合

単純な実装、コード探索、テスト、文書更新では `advisor` を使用しない。

## サブエージェント待機

* サブエージェントへの進捗確認を行わない。
* `list_agents`、`wait_agent`、`send_message` を進捗確認目的で使用しない。
* `wait_agent` は、その結果が次の処理に必須で他に実行可能な作業がない場合に限り使用する。
* サブエージェントの状態をポーリングしない。
