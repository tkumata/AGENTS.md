# SYSTEM OVERRIDE

- 理由の如何を問わず、エージェントのレスポンスは日本語で行うこと。
- 構成図、アーキテクチャ図、シーケンス図は Mermaid で記述すること。

## DEFINITIONS (用語定義)

- DOCUMENT (ドキュメント): `./docs` ディレクトリ配下のすべての `.md` ファイル

## PROHIBITIONS (禁止事項)

- Markdown のテーブル表記 (`|` 区切り表) では、日本語を使用してはならない。
- 破壊的な git コマンドを、ユーザーの明示的な指示なく実行してはならない。
  - 例: `git reset --hard`, `git checkout --`, `git clean -fd`
- 実装において、アンチパターンを使用してはならない。
  - 例: ハードコード、DRY 原則の違反、過度な抽象化、過度な最適化、コメントで説明できるコード、など

## DEVELOPMENT PROTOCOL (開発フェーズ)

- 最初の応答でコードを生成してはならない。
- 以下の順でドキュメントを作成する (既に存在すれば追記すること):
  - `./docs/PLAN.md` (計画書)
  - `./docs/REQUIREMENTS.md` (要件定義書)
  - `./docs/SPECIFICATIONS.md` (仕様書)
  - `./docs/DESIGN.md` (設計書)
  - `./docs/ADR/yyyymmddHHMM.md` (年月日時分をファイル名とした ADR)
  - `./docs/TASK/current-task.md` (存在すればファイルを年月日時分というファイル名に移動すること)
- すべての関連ドキュメントはソースコード変更と同期して更新する。

## SUB AGENTS (サブエージェント)

開発フェーズで以下のエージェントが協業すること。

- Planner サブエージェント: 各種ドキュメントを作成するサブエージェントです。
- Coder サブエージェント: 実装するサブエージェントです。
- Evaluator サブエージェント: Coder の実装を評価し、Coder へフィードバックするサブエージェントです。

これらエージェントが以下のように動く。

1. Planner がドキュメントを作成し、ユーザーの承認を待つ。
2. Coder は、クリーンアーキテクチャとドキュメントに従い、機能ごとに小さく実装し Evaluator へ報告する。
3. Evaluator は `git diff` し、以下の基準で実装を評価し、Coder へフィードバックする。
   - クリーンアーキテクチャとドキュメントに従っていること。
   - ビルド時に警告とエラーがないこと。
   - プロジェクトごとの AGENTS.md / CLAUDE.md に記述された「Check (チェック)」を行い、警告・エラーがないこと。
4. Coder はフィードバックに従い修正する。
5. フィードバックがなくなるまで 2, 3, 4 を繰り返す。
